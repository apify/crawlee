import { BasicCrawler, BasicCrawlerOptions } from "../../..";

interface RequestData {
    myValue: string;
}

interface SessionData {

}

describe('PuppeteerCrawler TS', () => {
    test('generics', () => {

    });
});
